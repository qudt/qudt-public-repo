@prefix dcterms: <http://purl.org/dc/terms/> .

# Exact added triples that inspection should allow for datatype schema.
# Keep this file small and explicit; add entries only when intentionally approved.
